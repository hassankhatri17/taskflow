# Copy to .env for local dev. Vite only exposes variables prefixed VITE_.

# Local dev (backend running on localhost:4000):
VITE_API_URL=http://localhost:4000/api

# Production example — point this at your deployed backend:
# VITE_API_URL=https://taskflow-api.onrender.com/api
